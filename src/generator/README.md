# Generator boundary

This directory is reserved for future procedural generator logic. Keep generator code independent from the React UI in `src/app`.

No game mechanics, case generation, or game data models belong here yet.
